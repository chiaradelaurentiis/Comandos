Te lista los archivos modificados:
    git status 

Agregar los archivos modificados: 
    git add . 

Commit
    git commit -m "Descripcion"

Sube en memoria todos los archivos que cambiaste y te limpia el git
    git stash -u 

Baja de memoria los archivos subidos con git stash -u
    git stash pop

Elina el ultimo commit y lo deja en changes 
    git reset --soft HEAD~1


Enviar tus cambios al repositorio remoto:
    git push origin nombre-de-la-rama


Actualizar tu copia local con los últimos cambios del repositorio remoto:
    git pull origin nombre-de-la-rama

verificar y cambiar de rama usando:
    git checkout nombre-de-la-rama

Para ver en qué rama estás actualmente:
    git branch

hola
